<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".DashboardActivity">

    <android.support.v7.widget.Toolbar
            android:id="@+id/dashboard_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?android:actionBarSize"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            android:background="@color/colorPrimaryDark"
            android:paddingStart="60dp"
            android:paddingLeft="60dp"
            app:title="@string/dashboard"

    />


    <android.support.v7.widget.RecyclerView
            android:layout_below="@+id/dashboard_toolbar"
            android:id="@+id/rv_dashboard"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>


    <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab_dashboard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:src="@drawable/plus"
            android:layout_margin="16dp"
            app:backgroundTint="@color/colorPrimaryDark"/>
    <Button
            android:layout_width="51dp"
            android:layout_height="wrap_content"
            android:background="@drawable/icon_menu"
            android:id="@+id/imageView2" android:clickable="true"
            android:onClick="openNavLayout"
            android:layout_alignLeft="@+id/dashboard_toolbar" android:layout_marginTop="5dp"/>

</RelativeLayout>