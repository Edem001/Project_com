<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.students.todolist_fixed">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
    android:testOnly="false">
        <activity android:name="com.example.students.todolist_fixed.preferencesActivity">
        </activity>
        <activity android:name="com.example.students.todolist_fixed.activityAboutUs">
        </activity>
        <activity android:name="com.example.students.todolist_fixed.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.example.students.todolist_fixed.DashboardActivity">
        </activity>
        <activity android:name="com.example.students.todolist_fixed.ItemActivity">
        </activity>
    </application>

</manifest>